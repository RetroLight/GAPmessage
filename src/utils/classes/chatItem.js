export class ChatItem {
    constructor(chatId) {
        this.chatId = chatId + '@c.us';
        this.contactCountry = 'Russia';
        this.chatMessagesList = [];
        this.currentMessage = '';
    }
}
